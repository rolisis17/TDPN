package types

const (
	// ModuleName is the canonical module identifier.
	ModuleName = "vpnbilling"
	// StoreKey is the base key used by future Cosmos SDK KV stores.
	StoreKey = ModuleName
	// RouterKey is reserved for message routing in future Cosmos SDK wiring.
	RouterKey = ModuleName
)
