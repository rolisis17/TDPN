# x/vpnsponsor

Scope:
- sponsor account permissions
- session credit delegation to end users/apps

Required properties:
- explicit sponsor authorization boundary
- reservation lifecycle visibility (`pending|submitted|confirmed|failed`)
- compatibility with issuer sponsor APIs (`/v1/sponsor/*`)