# GPM Productization Status Tracker

Last updated: 2026-04-23

Scope:
- Global Private Mesh (GPM) track only.
- Status labels below are based on repository evidence, not intent.

Compatibility note:
- legacy TDPN path aliases are retained for backward compatibility.
- Public route/profile naming stays `1hop|2hop|3hop`, with compatibility aliases `speed|balanced|private` and legacy `fast|privacy`; `speed-1hop` remains the explicit experimental alias.

## Completed

- M0 planning and safety baseline is in repo: `docs/global-privacy-mesh-track.md`, `docs/client-safety-guide.md`, `docs/exit-node-safety-baseline-v1.md`, and `docs/exit-node-safety-guide.md`. Acceptance criterion: the role model, safety posture, and non-goals are documented for operators and clients.
- Public path-profile contract and alias retention are implemented across scripts: `scripts/easy_node.sh`, `scripts/beta_pilot_runbook.sh`, `scripts/client_vpn_smoke.sh`, `scripts/client_vpn_profile_compare.sh`, `scripts/profile_compare_local.sh`, `scripts/profile_compare_trend.sh`, `scripts/profile_compare_campaign.sh`, `scripts/profile_compare_docker_matrix.sh`, and `scripts/three_machine_docker_profile_matrix.sh`. Acceptance criterion: profile input normalizes consistently and `1hop` never silently becomes the default.
- Validation and decision-support harnesses exist for profile comparison: `scripts/profile_compare_local.sh`, `scripts/profile_compare_trend.sh`, `scripts/profile_compare_campaign.sh`, `scripts/profile_compare_campaign_check.sh`, `scripts/profile_compare_campaign_signoff.sh`, `scripts/client_vpn_profile_compare.sh`, `scripts/three_machine_docker_profile_matrix.sh`, `scripts/three_machine_docker_profile_matrix_record.sh`, `scripts/three_machine_docker_readiness.sh`, and `scripts/single_machine_prod_readiness.sh`. Acceptance criterion: the repo can emit JSON and markdown summary artifacts for repeated comparisons and readiness checks.
- Local control API and desktop integration scaffolding are present: `docs/local-control-api.md`, `scripts/easy_node.sh` (`local-api-session`), `scripts/windows/local_api_session.ps1`, `scripts/integration_local_api_config_defaults.sh`, `scripts/integration_local_control_api_contract.sh`, and `scripts/integration_desktop_scaffold_contract.sh`. Acceptance criterion: `connect`, `disconnect`, `status`, `set_profile`, `get_diagnostics`, and `update` flow through one shared profile contract.
- Local control API production hardening now enforces fail-closed behavior for malformed production/security policy booleans and trusted-manifest legacy-connect safety checks in `services/localapi/service.go` with coverage in `services/localapi/service_test.go`. Acceptance criterion: malformed boolean env policy values cannot silently disable production protections, and session-based connect cannot bypass trusted-manifest policy via manual invite override.
- Profile-compare campaign-check summaries now expose deterministic M4 gate evaluation for micro-relay/trust-tier policy (`decision_diagnostics.m4_policy.gate_evaluation` + `gate_summary`) with per-gate status, actionable fail reasons, richer observed M4 diagnostics (`observed.micro_relay_policy_evidence.campaign_summary_details` and `selected_summaries_aggregate`), and stricter fail-closed evidence parsing/selected-summary coverage semantics in `scripts/profile_compare_campaign_check.sh`, covered by `scripts/integration_profile_compare_campaign_check.sh`. Acceptance criterion: operators can consume micro-relay/trust-tier NO-GO causes and underlying scoring/adaptive/trust evidence without manual log parsing.
- Roadmap/readiness freshness fail-closed behavior is hardened in shipped scripts: `scripts/roadmap_progress_report.sh` and `scripts/single_machine_prod_readiness.sh` now treat malformed or non-trustworthy freshness evidence as stale/unknown instead of silently fresh, with regression coverage in `scripts/integration_roadmap_progress_report.sh` and `scripts/integration_single_machine_prod_readiness.sh`. Acceptance criterion: invalid freshness metadata cannot silently pass roadmap or single-machine gates.
- Roadmap evidence-pack publication readiness surfacing is now wired for M2/M4/M5 tracks in `scripts/roadmap_progress_report.sh`, with deterministic regression coverage in `scripts/integration_roadmap_progress_report.sh`: `profile_default_gate_evidence_pack`, `runtime_actuation_promotion_evidence_pack`, and `profile_compare_multi_vm_stability_promotion_evidence_pack` now publish fail-closed status (`missing|invalid|stale|fail|pass`), helper availability, and concise next-action commands/reasons when actionable helpers are present. Acceptance criterion: roadmap and report artifacts expose machine-readable publication-readiness/actionability without manual log triage.
- Easy-node wrapper/operator help is now aligned for roadmap actionable runners in `scripts/easy_node.sh`: `roadmap-evidence-pack-actionable-run` and `roadmap-live-evidence-actionable-run` now document their actionable flags (`--roadmap-summary-json`, `--roadmap-report-md`, timeout/refresh/parallel controls, and evidence-pack `--scope`) and summary schema ids, with forwarding/help contract coverage in `scripts/integration_easy_node_roadmap_evidence_pack_actionable_run.sh` and `scripts/integration_easy_node_roadmap_live_evidence_actionable_run.sh`. Acceptance criterion: operators can discover actionable-run controls from easy-node help without opening helper scripts.
- Evidence-pack actionable selection now supports `--scope auto` in `scripts/roadmap_evidence_pack_actionable_run.sh`: pending roadmap next-action families are inferred deterministically (`profile-default`, `runtime-actuation`, `multi-vm`), mixed-family pending sets resolve to all relevant families only, and no recognized family resolves to none; summary output now surfaces `inputs.resolved_scope` and `inputs.scope_inference_reason` (mirrored under `roadmap.*`) with regression coverage in `scripts/integration_roadmap_evidence_pack_actionable_run.sh`. Acceptance criterion: auto selection avoids accidental generic `_evidence_pack` fanout while preserving explicit scope behavior.
- Combined live-and-pack orchestration is now available via `scripts/roadmap_live_and_pack_actionable_run.sh` and `./scripts/easy_node.sh roadmap-live-and-pack-actionable-run`: live-evidence and evidence-pack actionable runners are chained with deterministic per-stage summaries and fail-closed default semantics (`--continue-on-live-fail 0`), with coverage in `scripts/integration_roadmap_live_and_pack_actionable_run.sh` and `scripts/integration_easy_node_roadmap_live_and_pack_actionable_run.sh`. Acceptance criterion: operators can run one command for the full M2/M4/M5 live+publish loop.

## In-Progress

- M1 dedicated micro-relay role runtime admission is partially implemented at minimum viable level: provider descriptor role + entry admission are in scope, with post-merge evidence expected in `services/directory/service.go`, `services/directory/provider_api_test.go`, `services/entry/service.go`, and `services/entry/path_open_test.go`. Acceptance criterion: micro-relay nodes can be advertised and admitted through entry path-open flows without changing exit or validator behavior. Remaining work: scheduler/path-selection adoption, operator/runtime controls, and end-to-end validation artifacts.
- M2 selection-policy artifact evidence and policy-aware signoff checks are partially implemented across the compare pipeline, wrapper gates, profile-default runtime wiring, repeated-run automation scaffold, and roadmap/readiness surfacing: `scripts/profile_compare_local.sh`, `scripts/profile_compare_trend.sh`, `scripts/profile_compare_campaign.sh`, `scripts/profile_compare_campaign_check.sh`, `scripts/profile_compare_campaign_signoff.sh`, `scripts/profile_default_gate_run.sh`, `scripts/profile_default_gate_stability_run.sh`, `scripts/single_machine_prod_readiness.sh`, `scripts/roadmap_progress_report.sh`, `scripts/easy_node.sh`, `internal/app/config_file.go`, and integrations `scripts/integration_profile_compare_local.sh`, `scripts/integration_easy_node_client_profile_env.sh`, `scripts/integration_client_vpn_path_profile_wiring.sh`, `scripts/integration_profile_default_gate_stability_run.sh`, `scripts/integration_easy_node_profile_default_gate_stability_run.sh`, `scripts/integration_easy_node_profile_default_gate_stability_check.sh`, `scripts/integration_single_machine_prod_readiness.sh`, and `scripts/integration_roadmap_progress_report.sh` now propagate `.summary.selection_policy` (`sticky_pair_sec`, `entry_rotation_sec`, `entry_rotation_jitter_pct`, `exit_exploration_pct`, `path_profile`), expose check/signoff evidence booleans, enforce profile-derived defaults in runtime wrappers, aggregate repeated profile-default stability evidence, and include easy-node stability check gate wrapper coverage. A one-command cycle orchestrator path for real-host evidence collection is available via `./scripts/easy_node.sh profile-default-gate-stability-cycle ...`. `profile_default_gate_stability_check_summary.json` is now surfaced through roadmap + single-machine outputs, and manual-validation ingestion is now wired in `scripts/manual_validation_status.sh` + `scripts/manual_validation_report.sh` with integration coverage. Acceptance criterion: profile-compare local/trend/campaign/signoff and roadmap/readiness artifacts expose machine-readable policy evidence without manual log scraping, with fail-closed controls available at operational wrappers. Remaining work: capture/publish repeated real-host evidence proving strict defaults remain stable under live campaign refresh.
- M3 route-policy and 3-hop validation are partially wired: `scripts/integration_client_3hop_runtime.sh`, `scripts/ci_phase1_resilience.sh`, `scripts/ci_local.sh`, `scripts/beta_preflight.sh`, `scripts/three_machine_docker_profile_matrix.sh`, and `scripts/three_machine_docker_readiness.sh` exercise 3-hop and resilience paths, but the repo still describes true multi-host production signoff as pending in `docs/testing-guide.md`. Acceptance criterion: 3-hop can be validated repeatedly with deterministic artifacts in real-host conditions, not just local or docker rehearsal.
- Default-profile evidence gathering is in place but still under decision support rather than promotion: `scripts/profile_compare_local.sh`, `scripts/profile_compare_trend.sh`, `scripts/profile_compare_campaign.sh`, and `scripts/profile_compare_campaign_check.sh` can recommend `balanced`, but the docs still frame the result as comparative support rather than a finalized architecture shift. Acceptance criterion: repeated campaigns converge on a documented default decision with no manual interpretation.
- Runtime-promotion hardening slice is active: deterministic blockchain gate selector/freshness behavior is being tightened in `scripts/roadmap_progress_report.sh`, with expanded freshness regression coverage in `scripts/integration_roadmap_progress_report.sh`, `scripts/integration_single_machine_prod_readiness.sh`, `scripts/integration_phase5_settlement_layer_summary_report.sh`, and `scripts/integration_phase6_cosmos_l1_summary_report.sh`; this slice now also includes evidence-pack publication-readiness surfacing/actions for M2/M4/M5 in roadmap outputs. Acceptance criterion: promotion/readiness surfacing stays fail-closed under invalid, missing, null/empty, or conflicting timestamp evidence.
- Roadmap next-actions tooling now includes batch helper accelerators in `scripts/roadmap_progress_report.sh`: `roadmap_live_evidence_actionable_run`, `roadmap_evidence_pack_actionable_run`, and `roadmap_live_and_pack_actionable_run`, plus helper-oriented entry points `roadmap-live-evidence-cycle-batch-run` and `roadmap-validation-debt-actionable-run`. Deconflict behavior in `scripts/roadmap_next_actions_run.sh` prevents duplicate execution when combined and individual helpers coexist, with regression coverage in `scripts/integration_roadmap_next_actions_run.sh` and `scripts/integration_roadmap_progress_report.sh`. Acceptance criterion: operators can run batch helpers without duplicate command fanout while keeping evidence collection deterministic.

## Missing / Next

Next wave, in order:
- M2: run and archive real-host stability cycles for strict selection-policy defaults, then publish evidence-pack artifacts from those archived outputs. Current wiring is in `internal/app/client.go`, `internal/app/config_file.go`, `scripts/easy_node.sh`, `scripts/profile_default_gate_stability_run.sh`, and `scripts/roadmap_progress_report.sh`.
- M4: finish operator-enforcement/runtime scheduler linkage, then capture real-host promotion/demotion evidence for runtime actuation. Current wiring is in `services/directory/service.go`, `services/entry/service.go`, and `scripts/roadmap_progress_report.sh`.
- M5: execute live multi-VM sweep/stability cycles, reduce the outputs, and publish promotion evidence-pack artifacts. Current tooling is in `scripts/easy_node.sh` and `scripts/roadmap_progress_report.sh`.
- Tooling note: roadmap output now surfaces batch launchers (`roadmap_live_evidence_actionable_run`, `roadmap_evidence_pack_actionable_run`, `roadmap_live_and_pack_actionable_run`, `roadmap-live-evidence-cycle-batch-run`, and `roadmap-validation-debt-actionable-run`) to reduce manual per-action invocation. These are accelerators only; they do not replace required real-host evidence capture/publish, which remains the blocker for M2/M4/M5 promotion closure.

- M1 and M3 remain validation debt, but they are lower priority than the live-evidence tracks above.
